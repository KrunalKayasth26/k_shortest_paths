# k_shortest_paths
All-Pairs K-Shortest Paths with distinct effective (fault-tolerant) routing paths in a weighted network graph using Python &amp; NetworkX and gives routing distinct effective paths (fault tolerance) + CSV/Excel export.
